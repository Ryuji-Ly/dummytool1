# Keycloak Configuration
# After setting up Keycloak (see README.md), fill in these values:

# The Client ID you created in Keycloak (e.g., "dummytool1")
KEYCLOAK_CLIENT_ID=dummytool1

# Get this from Keycloak: Clients > your-client > Credentials tab
KEYCLOAK_CLIENT_SECRET=your-client-secret-from-keycloak

# Format: http://localhost:8080/realms/YOUR-REALM-NAME
# If you followed the guide, use: http://localhost:8080/realms/dummytool1
KEYCLOAK_ISSUER=http://localhost:8080/realms/dummytool1

# NextAuth Configuration

# Your application URL (for local development, use http://localhost:3000)
NEXTAUTH_URL=http://localhost:3000

# Generate a secure random secret using PowerShell:
# $bytes = New-Object byte[] 32; $rng = [System.Security.Cryptography.RandomNumberGenerator]::Create(); $rng.GetBytes($bytes); [Convert]::ToBase64String($bytes)
# Or use: https://generate-secret.vercel.app/32
NEXTAUTH_SECRET=your-generated-secret-here
